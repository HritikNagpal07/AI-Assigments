pip install transformers==4.55.2 peft==0.17.0 accelerate==1.10.0
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu  # CPU version
pip install fastapi uvicorn datasets sentencepiece protobuf
pip install streamlit  # For frontend
